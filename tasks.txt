**Create Role Entity and set routes:
GET /all-routes

**Create FordModels Entity and set routes:
POST /add-new-model 
GET /all-models

**Create Servicii Entity and set routes:
POST /add-new-service
DELETE /delete-specific-service
GET /all-services
PATCH /specific-service/:serviciu_id